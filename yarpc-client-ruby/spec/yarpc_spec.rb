# frozen_string_literal: true

RSpec.describe Yarpc do
  it "has a version number" do
    expect(Yarpc::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
